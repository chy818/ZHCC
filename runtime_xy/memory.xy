/**
 * @file runtime_xy/memory.xy
 * @brief XY 语言内存管理运行时
 * @description 用 XY 语言实现的内存管理函数
 */

/**
 * 分配并初始化为零的内存
 * @param size 大小（字节）
 * @return 内存指针
 */
函数 内存分配零(大小: 整数): 任意 {
    返回 外部 "malloc"(大小)
}
